using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterList : MonoBehaviour
{
    public CharacterSo[] characterSo;

    private void Start()
    {
        for (int i = 0; i < characterSo.Length; i++)
        {
            if (characterSo[i] != null)
            {

            }
        }
    }
}
